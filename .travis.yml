language: bash

dist: focal
addons:
    apt:
        packages:
            makeself

stages:
    build:

os:
    - linux

script:
    - make

deploy:
    provider: releases
    file_glob: true
    file: ktz-env-*.run
    skip_cleanup: true
    on:
        tags: true
